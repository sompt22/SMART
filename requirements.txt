# DEPRECATED — this file is not used by any automated build.
# Use environment.yml (Linux/CUDA) or environment_mac_m1.yml (Apple Silicon) instead.
# Kept as a reference only; package versions here are not pinned and may be out of date.
#
# conda update -n base -c defaults conda
# CUDA 11.3
#conda install pytorch==1.12.1 torchvision==0.13.1 torchaudio==0.12.1 cudatoolkit=11.3 -c pytorch
# CUDA 11.6
#conda install pytorch==1.12.1 torchvision==0.13.1 torchaudio==0.12.1 cudatoolkit=11.6 -c pytorch -c conda-forge
#conda install pytorch==1.13.1 torchvision==0.14.1 torchaudio==0.13.1 pytorch-cuda=11.6 -c pytorch -c nvidia
# CUDA 11.7
#conda install pytorch==1.13.1 torchvision==0.14.1 torchaudio==0.13.1 pytorch-cuda=11.7 -c pytorch -c nvidia

## conda install cython
## conda install ninja
## conda install cudatoolkit-dev -c conda-forge
#pip install python-dev-tools --user --upgrade
numpy==1.23.5
opencv-python
Cython
numba
progress
matplotlib
easydict
scipy
pyyaml
yacs
cython-bbox
motmetrics
lapx>=0.5.2
openpyxl
Pillow
tensorboardX
fvcore
pycocotools
pyquaternion
pyrsistent
